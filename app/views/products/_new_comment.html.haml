- if signed_in?
	%h4 Add a Review
	= form_for([@product, @product.comments.build]) do |f|
		%p
			= f.label :body, "Comments"
			%br
			= f.text_area :body
		%p
			= f.label :rating
			%br
			= f.text_field :rating, class: "rating"
		%p= f.submit "Submit" 